{
  "compilerOptions": {
    // все скомпилированные файлы в ./build
    "outDir": "./built",
    // Принять файлы JavaScript в качестве входных данных
    "allowJs": true,
    // Компиляция в формат
    "target": "es5"
  },
  // чтение `./src`
  "include": [
    "./src/**/*"
  ]
}